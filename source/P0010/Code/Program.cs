using System;


namespace P0010
{
    class Program
    {
        static void Main()
        {
            // Note: this project is broken *on purpose* to provide a project for build infrastructure testing that fails to compile.
            // DO NOT FIX.
            var output = ""

            Console.WriteLine(output);
        }
    }
}
